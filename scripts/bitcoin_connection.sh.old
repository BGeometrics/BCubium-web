#!/bin/bash

PORT=$(netstat -ap | grep bitcoind |grep "\*:[0-9]" | grep LISTEN | head -n 1 |  awk '{print $4}' | cut -b 3-)
BITCOIN_CONN=$(curl -sL https://bitnodes.earn.com/api/v1/nodes/me-$PORT/)

RET="${BITCOIN_CONN/true/\"true\"}" 
RET="${RET/false/\"false\"}" 

echo -n $RET

